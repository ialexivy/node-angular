(function() {
    'use strict';

    angular.module('nodeApp.core', [
        /*
         * Angular modules
         */
        'ngResource',
        /*
         * 3rd Party modules
         */
        'ui.router', // flexible routing framework which allows you to organize the parts of your interface into a state machine.   
        'ui.bootstrap', // ui-bootstrap (ex: carousel, pagination, dialog)   
    ]);
})();